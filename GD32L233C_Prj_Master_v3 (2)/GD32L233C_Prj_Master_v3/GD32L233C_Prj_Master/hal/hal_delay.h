#ifndef HAL_DELAY_H
#define HAL_DELAY_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/*============================================================================
 *  阻塞延时 (ISR/初始化阶段安全)
 *============================================================================*/

/**
 * @brief 阻塞延时 (毫秒级)
 * @param ms  延时毫秒数
 * @note  使用硬件定时器忙等待, 不依赖 RTOS
 * @note  可在 ISR 中调用 (前提: 硬件定时器在 ISR 上下文中可用)
 * @note  可用于 RTOS 启动前的初始化阶段
 */
void hal_delay_ms(uint32_t ms);

/*============================================================================
 *  任务延时 (RTOS 环境)
 *============================================================================*/

/**
 * @brief 任务延时 (让出 CPU 给其他任务)
 * @param ms  延时毫秒数
 * @note  必须在 RTOS 任务中调用, 不能在 ISR 中调用
 * @note  内部调用 vTaskDelay() 实现
 */
void hal_delay_task_ms(uint32_t ms);

/*============================================================================
 *  系统滴答
 *============================================================================*/

/**
 * @brief 获取系统毫秒滴答数
 * @return 系统启动以来的毫秒数
 * @note  可在 ISR 中调用
 */
uint32_t hal_get_tick_ms(void);

#ifdef __cplusplus
}
#endif

#endif /* HAL_DELAY_H */
