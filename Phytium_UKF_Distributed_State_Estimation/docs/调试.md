# 联合调试记录

> 两个AI协作调试 Phytium PE2204 LoRa 主控接收链路

## 当前状态 (2026-05-26)

### ✅ Linux 侧 LoRa 接收已跑通！

30秒内收到 **14包 / 1792字节**，GD32协议帧 `AA 55` 头正确。

---

## Linux 侧方案 — 已验证通过的配置

### 硬件连接

| LoRa 模块 | 飞腾派 J1 | 物理引脚 | 功能 |
|-----------|----------|---------|------|
| VCC | Pin1 | 3.3V | 电源 (3.3V!) |
| GND | Pin6 | GND | 地 |
| RXD | Pin8 | UART2_TXD | LoRa收 ← 飞腾发 |
| TXD | Pin10 | UART2_RXD | LoRa发 → 飞腾收 |
| MD0 | Pin11 | GPIO3_1 | 模式控制 |
| AUX | Pin7 | GPIO2_10 | 忙闲检测 |

### UART 端口: `/dev/ttyAMA2` (不是 ttyAMA3!)

**重要纠正**: J1 Pin8/Pin10 是 **UART2** (`/dev/ttyAMA2`, `0x2800E000`)，不是 UART3。之前所有文档标 UART3 都是错的。

验证通过：对 `/dev/ttyAMA2` 115200-8N1 的 TX/RX 均正常。

### GPIO 控制

- **FIOPAD IOMUX 是必须的** — 这是之前 FreeRTOS 侧 GPIO_DDR 写不进去的根本原因
- GPIO3_1 (MD0): FIOPAD_C49_REG0_OFFSET (0x00E0) → func6
- GPIO2_10 (AUX): FIOPAD_A37_REG0_OFFSET (0x00C4) → func6
- MD0 高低电平切换已验证通过
- AUX 变化已验证（0.7s/3.6s 短暂 HIGH → 收到 RF 数据）

### AT 命令格式

此 LoRa 模块使用 **ATK 原厂格式** (atk_mwcc68d.c 风格)，不是 GD32 BSP 格式：

| 参数 | 正确命令 | GD32 BSP命令 | 结果 |
|------|---------|-------------|------|
| 地址 | `AT+ADDR=00,0B` | `AT+ADDR=00,0B` | ✅ 两者一致 |
| 信道+速率 | `AT+WLRATE=23,5` | `AT+CHN=23` + `AT+WLRATE=5` | ❌ BSP格式返回ERROR |
| 功率 | `AT+TPOWER=4` | `AT+POWER=4` | ❌ BSP格式返回ERROR |
| 波特率 | `AT+UART=7,0` | `AT+BPS=7` | ❌ BSP格式返回ERROR |
| 保存 | `AT+FLASH=1` | 无 | — |

### AUX 排查结论

AUX 引脚完全正常。之前 AUX 一直 LOW 的原因：退出 AT 模式后只等了 1 秒，模块还没完成重启。

**正确做法（照搬 GD32 `LoRa_ExitConfigMode`）**：退出 AT 模式后等待 AUX 变 HIGH，最多等 5 秒。

---

## GD32 终端节点帧格式 (已验证逐字节匹配)

### 发送流程
```
GD32终端 → LoRa模块(UART):  [00 0B 17] [AA 55 00 89 00 1C AB B7 04 ...]
                             ↑LoRa RF头  ↑GD32协议帧
                             3字节      137字节

LoRa模块无线发送 (TMODE=1定点):  RF目标=0x000B, 信道=23, 载荷=GD32协议帧

主控LoRa模块接收 → UART输出:  AA 55 00 89 00 1C AB B7 04 ...  (剥掉了RF头)
```

### GD32协议帧内部结构
```
偏移 | 字节数 | 字段        | 示例值 (PKT #5)
-----|--------|-------------|------------------
  0  |   2    | Frame头     | AA 55
  2  |   2    | 数据长度*   | 00 89 = 137
  4  |   2    | 设备ID      | 00 1C = 0x001C
  6  |   2    | 时间戳      | AB B7
  8  |   1    | 帧类型      | 04 (0x04=加密数据帧, 0x01=短状态帧)
  9  |   4    | Sync加密同步码 | 43 50 A3 21 = 0x4350A321  ★对照点
 13  |  128   | 加密载荷    | 05 C5 6B 31 34 CE 97 AE ...

*数据长度 = 从设备ID到末尾的总字节数 (包含sync+payload)
```

### 逐字节匹配验证

终端发送日志:
```
Encrypted: sync=0x4350A321, len=132
  0x05 0xC5 0x6B 0x31 0x34 0xCE 0x97 0xAE 0x3A 0x8D 0x01 0xFE ...
```

接收 (PKT #5):
```
[SYNC] 0x4350A321 ← 对照终端 "Encrypted: sync=0x4350A321"
[PLD ] 05 C5 6B 31 34 CE 97 AE 3A 8D 01 FE C3 F6 F8 FC C2 D9 ED 2B 13 AC 01 7D DA 25 A8 8C AF 69 80 66 ...
```

**对照方式**：在终端日志找 `Encrypted: sync=0xNNNNNNNN`，然后在接收输出找 `[SYNC] 0xNNNNNNNN`，payload 逐字节一致即确认通信正确。

### 已确认匹配的帧
- `sync=0x4350A321` ✅ 逐字节一致
- `sync=0x8000E09F` ✅ 逐字节一致
- `sync=0xFCA9A7CE` ✅ 等均可匹配

### 两种帧类型
- **type=0x01**: 短帧 (25字节)，可能是心跳/状态包
- **type=0x04**: 加密数据帧 (137字节)，正常上报数据

---

## 可执行程序

编译好的程序在开发板 `/tmp/lora_receiver`。

运行方式：
```bash
# 在开发板上
sudo /tmp/lora_receiver
# Ctrl+C 停止
```

源码位置：
- 本地: `/home/alientek/Phytium/src/linux-app/lora_receiver.c`
- 开发板: `/tmp/lora_receiver_new.c`

编译方式（开发板本地gcc）：
```bash
gcc -Wall -O2 -o /tmp/lora_receiver /tmp/lora_receiver_new.c
```

---

## 后续移植到 FreeRTOS 的注意事项

1. **UART 端口**: 使用 UART2 (0x2800E000)，不是 UART3
2. **GPIO**: 必须先配置 FIOPAD IOMUX 再操作 GPIO 寄存器
3. **Linux 冲突**: UART2 也被 Linux 设备树占用(`status=okay`)，需禁用或处理
4. **AT 格式**: 以模块实际响应的 ATK 格式为准

---

## 源码位置

| 文件 | 路径 | 说明 |
|------|------|------|
| lora_receiver.c | `/home/alientek/Phytium/src/linux-app/lora_receiver.c` | Linux侧LoRa接收程序(已验证) |
| aux_check.c | `/home/alientek/Phytium/src/linux-app/aux_check.c` | AUX引脚监测工具 |
| at_test.c | `/home/alientek/Phytium/src/linux-app/at_test.c` | AT命令兼容性测试 |
| hw_verify.c | `/home/alientek/Phytium/src/linux-app/hw_verify.c` | 硬件验证工具(UART+GPIO+IOMUX) |
| uart_test.c | `/home/alientek/Phytium/src/linux-app/uart_test.c` | UART简单发送测试 |
| mwcc68_app.c | `GD32.../BSP/mwcc68_app.c` | GD32 LoRa初始化流程参考 |
| mwcc68_cfg.h | `GD32.../BSP/mwcc68_cfg.h` | GD32 LoRa参数配置 |
| atk_mwcc68d.c | `GD32.../old/atk_mwcc68d.c` | ATK原厂驱动(已确认的AT格式) |
| fio_mux.c | `SDK/standalone/board/phytiumpi_firefly/fio_mux.c` | IOMUX引脚复用配置参考 |
| fparameters_comm.h | `SDK/standalone/soc/pe220x/fparameters_comm.h` | UART2基址=0x2800E000 |
